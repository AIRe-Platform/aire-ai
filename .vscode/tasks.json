{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Build Docker image",
            "command": "docker",
            "args": [
                "build",
                "${workspaceFolder}",
                "-t",
                "aire-id"
            ],
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
